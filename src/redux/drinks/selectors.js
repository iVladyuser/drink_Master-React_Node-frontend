export const selectCocktails = state => state.cocktails.cocktails;

export const selectTotalCocktails = state => state.cocktails.totalCocktails;

export const selectIsLoading = state => state.cocktails.isLoading;
