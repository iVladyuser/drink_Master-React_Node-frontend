export const selectCategories = state => state.filters.categories;

export const selectIngredients = state => state.filters.ingredients;

export const selectIsLoadingFilters = state => state.filters.isLoading;
